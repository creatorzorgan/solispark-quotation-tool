# Copy to .env.local and fill in — NEVER commit .env.local.
# Supabase URL + anon/publishable key (safe to expose in client code,
# but still kept in env for rotation).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_XXXXXXXXXXXXXXX
